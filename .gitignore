node_modules
.refs/
dist
# Secrets — never commit (Midtrans keys, etc.)
.env
.env.local
.env.*.local

# Supabase CLI temp/branch dirs
supabase/.branches/
supabase/.temp/

# Agent tooling dirs (may contain API keys/config — never commit)
.agents/
.codex/
